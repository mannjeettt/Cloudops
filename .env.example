# Frontend environment variables

# Authentication is required by default. Set to false only for local demo mode.
# Demo mode uses an in-memory session token generated in the browser.
VITE_REQUIRE_AUTH=true
